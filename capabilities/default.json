{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default permissions for the local desktop UI.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
